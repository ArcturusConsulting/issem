apiVersion: apps/v1
kind: Deployment
metadata:
  name: issem-gateway
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: issem-core
  template:
    metadata:
      labels:
        app: issem-core
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      imagePullSecrets:
      - name: ghcr-auth
      containers:
      - name: orchestrator
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: ISSEM_PLC_ENDPOINT
          value: {{ .Values.envOverrides.plcEndpoint | quote }}
        - name: ISSEM_REDIS_URL
          value: {{ .Values.envOverrides.redisUrl | quote }}
        livenessProbe:
          exec:
            command:
            - pidof
            - issem_core
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - name: global-config
          mountPath: /app/config.json
        - name: opc-mapping             # ◄ 1. Mounts the file inside the /app directory
          mountPath: /app/opc_ua_mapping.json
        - name: pki-store
          mountPath: /app/pki
        - name: license-token
          mountPath: /app/license.jwt
      volumes:
      - name: global-config
        hostPath:
          path: {{ .Values.hostPaths.configJson }}
          type: File
      - name: opc-mapping             # ◄ 2. References the mapping file's physical host path
        hostPath:
          path: {{ .Values.hostPaths.opcUaMappingJson }}
          type: File
      - name: pki-store
        hostPath:
          path: {{ .Values.hostPaths.pkiDir }}
          type: Directory
      - name: license-token
        hostPath:
          path: {{ .Values.hostPaths.licenseJwt }}
          type: File